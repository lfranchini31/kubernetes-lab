kind: Service
apiVersion: v1
metadata:
  name: frontend-ui
  labels:
    app: frontend-ui
    lab: kubernetes-lab
spec:
  type: LoadBalancer
  ports:
    - port: 80
      name: http-80
      targetPort: http
  selector:
    app: frontend-ui